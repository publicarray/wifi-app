name: Wails build

permissions:
  contents: write

on:
  push:
  pull_request:
  release:
    types: [created, published]

jobs:
  test:
    name: Test & lint (Linux)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Install Linux build dependencies
        # nl80211 + Wails desktop deps so go build / staticcheck can see all
        # platform-tagged Linux source files.
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            libgtk-3-dev libwebkit2gtk-4.0-dev pkg-config

      - name: go vet (default tags)
        run: go vet ./...

      - name: go vet (with iw tag)
        run: go vet -tags iw ./...

      - name: go test -race (default tags)
        run: go test -race -count=1 ./...

      - name: go test -race (with iw tag)
        run: go test -tags iw -race -count=1 ./...

      - name: Install staticcheck
        # Installed fresh per-run rather than pinned: staticcheck releases
        # follow Go releases closely and an out-of-date binary fails to read
        # newer Go .a files. Bump go-version above to bump the toolchain.
        run: go install honnef.co/go/tools/cmd/staticcheck@latest

      - name: staticcheck (default tags)
        run: $(go env GOPATH)/bin/staticcheck ./...

      - name: staticcheck (with iw tag)
        run: $(go env GOPATH)/bin/staticcheck -tags iw ./...

      - name: Cross-compile sanity (windows/amd64)
        env:
          GOOS: windows
          GOARCH: amd64
        run: go build ./...

      - name: Cross-compile sanity (darwin/arm64, no cgo)
        env:
          GOOS: darwin
          GOARCH: arm64
          CGO_ENABLED: "0"
        run: go build ./...

      - name: Cross-compile sanity (darwin/amd64, no cgo)
        env:
          GOOS: darwin
          GOARCH: amd64
          CGO_ENABLED: "0"
        run: go build ./...

  build:
    needs: test
    strategy:
      fail-fast: false
      matrix:
        build:
          [
            {
              name: wifi-app,
              platform: linux/amd64,
              os: ubuntu-latest,
              out: wifi-app-linux-amd64,
            },
            {
              name: wifi-app.exe,
              platform: windows/amd64,
              os: windows-latest,
              out: wifi-app-windows-amd64.exe,
            },
            {
              name: wifi-app,
              platform: darwin/universal,
              os: macos-latest,
              out: wifi-app-macos-universal.zip,
            },
          ]
    runs-on: ${{ matrix.build.os }}

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - uses: snider/build@main
        with:
          build-name: ${{ matrix.build.name }}
          build-platform: ${{ matrix.build.platform }}

      # -------------------------
      # Rename binaries
      # -------------------------
      - name: Rename binary (unix)
        if: runner.os == 'Linux'
        run: |
          mv build/bin/${{ matrix.build.name }} build/bin/${{ matrix.build.out }}

      - name: Rename binary (windows)
        if: runner.os == 'Windows'
        run: |
          Move-Item build\bin\${{ matrix.build.name }} build\bin\${{ matrix.build.out }}

      - name: Rename macOS zip
        if: runner.os == 'macOS'
        run: |
          mv build/bin/${{ matrix.build.name }}.app.zip build/bin/${{ matrix.build.out }}

      # -------------------------
      # Upload binaries to GitHub Release
      # -------------------------
      - name: Upload to GitHub Release
        if: github.event_name == 'release'
        uses: softprops/action-gh-release@v2
        with:
          files: build/bin/${{ matrix.build.out }}
